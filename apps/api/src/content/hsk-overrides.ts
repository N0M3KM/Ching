import type {Vocabulary} from '@ching/contracts';
// Exceptional corrections only. IDs must match the source word's generated HSK identity.
// Each override replaces one complete generated record and is validated with the output.
export const hskOverrides: readonly Vocabulary[] = [];
